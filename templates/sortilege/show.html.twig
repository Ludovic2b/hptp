{% extends 'base.html.twig' %}

{% block title %}Sortilege{% endblock %}

{% block body %}
    <h1>Sortilege</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sortilege.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sortilege.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sortilege_index') }}">back to list</a>

    <a href="{{ path('app_sortilege_edit', {'id': sortilege.id}) }}">edit</a>

    {{ include('sortilege/_delete_form.html.twig') }}
{% endblock %}
