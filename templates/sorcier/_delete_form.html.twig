<form method="post" action="{{ path('app_sorcier_delete', {'id': sorcier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sorcier.id) }}">
    <button class="btn">Delete</button>
</form>
